namespace FileSystem
{
    public class FinalExerciseTodo
    {
        // Make an application that given a directory and a Regex, it will check for all files that contain a match for that Regex recursively
        // The function should return the full name of all those files
        // Make sure you only check files that can be opened (not in use)
        // Keep in mind Memory usage

        public IEnumerable<string> GetMatches(string directory, string regexPattern)
        {
            throw new NotImplementedException("TODO");
        }
    }
}
